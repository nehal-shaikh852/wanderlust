<nav class="navbar navbar-expand-md bg-light border-bottom sticky-top " style="min-height: 100px;">
  <div class="container-fluid">
    <!-- Brand Logo -->
    <a class="navbar-brand d-flex align-items-center" href="/listings">
      <i class="fa-regular fa-compass me-2 fs-4"></i>
      <span class="fw-bold">Wanderlust</span>
    </a>

    <!-- Toggle button -->
    <button class="navbar-toggler" 
            type="button" 
            data-bs-toggle="collapse" 
            data-bs-target="#navbarNavAltMarkup" 
            aria-controls="navbarNavAltMarkup" 
            aria-expanded="false" 
            aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar content -->
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav me-auto">
        <a class="nav-link" href="/listings">Explore</a>
        <a class="nav-link" href="/listings/new">Add Listing</a>
      </div>

      <!-- Auth buttons -->
      <div class="navbar-nav ms-auto">
        <% if (!currUser) { %>
          <div class="d-flex flex-column flex-md-row gap-2">
            <a href="/login" class="btn btn-outline-dark">Log in</a>
            <a href="/signup" class="btn btn-danger">Sign up</a>
          </div>
        <% } else { %>
          <a href="/logout" class="btn btn-danger">Log out</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
